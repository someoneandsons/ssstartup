{{	text.p(
	form.range(forms.range.name, forms.range.value, forms.range.options)
) }}
{{	text.p(
	form.number(forms.number.name, forms.number.value)
) }}
{{	text.p(
	form.color(forms.color.name, forms.color.value)
) }}
{{	text.p(
	form.date(forms.date.name, now|date('Y-m-d'))
) }}
{{	text.p(
	form.month(forms.month.name, now|date('Y-m'))
) }}
{{	text.p(
	form.week(forms.week.name, now|date('Y-\\WW'))
) }}
{{	text.p(
	form.datetime(forms.datetime.name, now|date('c'))
) }}
{{	text.p(
	form.datetime_local(forms.datetime_local.name, now|date('Y-m-d\\TH:i:s'))
) }}